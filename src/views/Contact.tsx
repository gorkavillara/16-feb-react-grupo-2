import React from "react"
import styles from "./Contact.module.css"

const Contact = () => {
    return (
        <div>
            <h1 className={`${styles["mitad"]} ${styles["tituloContact"]}`}>
                Hola desde contact
            </h1>
        </div>
    )
}

export default Contact
